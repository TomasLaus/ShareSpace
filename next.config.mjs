/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: 'superb-rook-557.convex.cloud',
      },
    ],
  },
};

export default nextConfig;
